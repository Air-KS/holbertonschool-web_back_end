// Script that displays the number of documents in the collection school
// The database name will be passed as option of mongo command
const documentCount = db.scholl.count();
printjson(documentCount)
